import { HostlinkDirective } from './hostlink.directive';

describe('HostlinkDirective', () => {
  it('should create an instance', () => {
    const directive = new HostlinkDirective();
    expect(directive).toBeTruthy();
  });
});
